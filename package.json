{
  "private": true,
  "name": "hardware-wallets-sdk",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "postinstall": "rm -rf sdk/*/node_modules && ln -sfn ../node_modules demo-app/node_modules 2>/dev/null || true"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
